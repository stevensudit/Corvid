# CLAUDE.md

Two non-obvious types worth knowing upfront:
- `stable_ids<>` — dense ID-based storage with FIFO ID reuse
- `intern_table<>` / `interned_value<>` — singleton value storage with stable addresses (identity by pointer)
