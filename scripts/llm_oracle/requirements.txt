# Everything except torch, which setup.sh installs first from the CPU-only
# wheel index (the PyPI wheel drags in the CUDA libraries, several GB that
# the oracle never uses). Pinned so both platforms record the same versions
# in the manifest; bump them together and rerun the oracle on both.
transformers==5.17.0
safetensors==0.8.0
huggingface_hub==1.33.0
gguf==0.19.0
