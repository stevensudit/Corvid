{
  "configurations": [
    {
      "name": "Type lldb (using CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/debug_bin/${fileBasenameNoExtension}",
      "args": [],
      "stopOnEntry": false,
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "clang-build"
    }
  ]
}
