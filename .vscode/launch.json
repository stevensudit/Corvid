{
  "configurations": [
    {
      "name": "Type lldb (using CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/debug_bin/${fileBasenameNoExtension}.exe",
      "args": [],
      "stopOnEntry": false,
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build"
    }
  ]
}
